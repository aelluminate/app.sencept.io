import { Lora, Space_Grotesk, Inter } from "next/font/google"

export const lora = Lora({
  variable: "--font-lora",
  subsets: ["latin"],
})

export const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
})

export const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
})